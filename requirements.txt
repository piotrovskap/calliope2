# Python tooling for portal generators, validators, and DB harvest scripts.
# Bootstrap:  python3 -m pip install -r requirements.txt
# CI installs this (see .github/workflows/ci.yml).

PyYAML>=6.0            # spec frontmatter parsing (specs/_gen.py)
jsonschema>=4.0        # golden-record + portal schema validation (now required)
python-dotenv>=1.0     # .env loading for DB/Slack scripts
tabulate>=0.9          # table rendering in inventory/report scripts

# DB harvest drivers — needed only for live SQL Server / ODBC extraction,
# not for portal generation or CI. Install locally when harvesting:
#   pip install pymssql pyodbc
